package com.tutu.reading.leetcode.string;

/**
 * @Description:
 * 格式规则描述如下：
 * 字符串由一系列“元素”组成，每个元素之间用英文逗号分隔
 * 元素可以是一个英文字符串（例如 abc ），也可以是几个英文字符串，之间用逗号分隔（例如 abc,efg ）
 * 可以在元素后面加上冒号加数字，表示多个元素，例如 :2
 * 现在的要求是，将整个字符串解析成一个list，需要将里面所有“元素”切分到最小单元
 *
 * 例如：
 *
 * 输入：ab,cd,ef
 * 返回：['ab', 'cd', 'ef']
 *
 * 输入：ab,cd:2,ef
 * 返回：['ab', 'cd', 'cd', 'ef']
 *
 * 输入：ab,(cd,ef):2,gh
 * 返回：['ab', ['cd', 'ef'], ['cd', 'ef'], 'gh']
 *
 * 要求：
 * 实现这个函数，输入是字符串，返回值是结果list
 * 如果输入字符串格式非法，应该返回false
 * 自行构造几组输入，验证函数的正确性
 *
 * @Author: hxf
 * @CreateDate: 2020/9/1$ 19:01$
 * @Version: 1.0
 */
public class StringSplit {

    public static void main(String[] args) {

    }

    













}
